#!/usr/bin/env python
# GENERATED BY INSIGHT-PLUGIN - DO NOT EDIT
import os
import json
from sys import argv

Name = "Rapid7 Velociraptor"
Vendor = "rapid7"
Version = "1.0.0"
Description = "Manage endpoint collections, hunts, and artifact deployments using the Rapid7 Velociraptor API"


def main():
    if 'http' in argv:
        if os.environ.get("GUNICORN_CONFIG_FILE"):
            with open(os.environ.get("GUNICORN_CONFIG_FILE")) as gf:
                gunicorn_cfg = json.load(gf)
                if gunicorn_cfg.get("worker_class", "sync") == "gevent":
                    from gevent import monkey
                    monkey.patch_all()
        elif 'gevent' in argv:
            from gevent import monkey
            monkey.patch_all()

    import insightconnect_plugin_runtime
    from icon_rapid7_velociraptor import connection, actions, triggers, tasks

    class ICONRapid7Velociraptor(insightconnect_plugin_runtime.Plugin):
        def __init__(self):
            super(self.__class__, self).__init__(
                name=Name,
                vendor=Vendor,
                version=Version,
                description=Description,
                connection=connection.Connection()
            )
            self.add_action(actions.GetClient())
        
            self.add_action(actions.ListClients())
        
            self.add_action(actions.GetClientLabels())
        
            self.add_action(actions.AddLabel())
        
            self.add_action(actions.RemoveLabel())
        
            self.add_action(actions.CreateFlow())
        
            self.add_action(actions.GetFlow())
        
            self.add_action(actions.ListFlows())
        
            self.add_action(actions.GetFlowResults())
        
            self.add_action(actions.CreateHunt())
        
            self.add_action(actions.GetHunt())
        
            self.add_action(actions.ListHunts())
        
            self.add_action(actions.GetHuntResults())
        
            self.add_action(actions.ListArtifacts())
        

    """Run plugin"""
    cli = insightconnect_plugin_runtime.CLI(ICONRapid7Velociraptor())
    cli.run()


if __name__ == "__main__":
    main()
